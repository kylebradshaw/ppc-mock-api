{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "for mocking",
  "main": "index.js",
  "scripts": {
    "mock": "json-server -w index.js -p 4001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sha1": "^1.1.1"
  }
}
